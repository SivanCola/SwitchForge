# Code owners for SwitchForge
#
# Ownership metadata for repository-wide changes and sensitive paths.
# Solo-maintainer branch protection requires CI but does not require a separate
# Code Owner approval, because GitHub does not allow self-approval.

# Global fallback.
*                 @SivanCola

# Most sensitive paths: CI/signing/release and the Rust backend.
/.github/         @SivanCola
/src-tauri/       @SivanCola
